kind = "web"
previewPath = "/"
title = "Multi-Model Chat"
version = "1.0.0"
id = "artifacts/chat-ui"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24918

[services.development]
run = "npm run -w @workspace/chat-ui dev"

[services.production]
build = [ "npm", "--prefix", "artifacts/chat-ui", "run", "build" ]
publicDir = "artifacts/chat-ui/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24918"
BASE_PATH = "/"
