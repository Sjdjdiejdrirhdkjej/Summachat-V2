{
  "installCommand": "npm install",
  "buildCommand": "NODE_ENV=production npm run -w @workspace/api-server build && VITE_ALLOW_SAME_ORIGIN_API=1 npm run -w @workspace/chat-ui build",
  "outputDirectory": "artifacts/chat-ui/dist/public",
  "functions": {
    "api/**/*.ts": {
      "maxDuration": 120,
      "memory": 1024
    }
  },
  "rewrites": [
    {
      "source": "/((?!api(?:$|/)).*)",
      "destination": "/index.html"
    }
  ]
}
